name: CKAD Lab 41

on:
  push:
    paths: ['ckad/41/**', '.github/workflows/ckad-lab-41.yml']
  pull_request:
    paths: ['ckad/41/**', '.github/workflows/ckad-lab-41.yml']

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create cluster
        uses: helm/kind-action@v1
        with:
          config: ckad/41/assets/kind-config.yaml
          cluster_name: ckad-lab-41

      - name: Apply solution
        run: bash ckad/41/assets/fix.sh

      - name: Run checks
        run: bash ckad/41/assets/check.sh

      - name: Tear down
        if: always()
        run: bash ckad/41/assets/cleanup.sh
