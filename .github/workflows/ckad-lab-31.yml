name: CKAD Lab 31

on:
  push:
    paths: ['ckad/31/**', '.github/workflows/ckad-lab-31.yml']
  pull_request:
    paths: ['ckad/31/**', '.github/workflows/ckad-lab-31.yml']

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Create cluster
        uses: helm/kind-action@v1
        with:
          config: ckad/31/assets/kind-config.yaml
          cluster_name: ckad-lab-31

      - name: Apply solution
        run: bash ckad/31/assets/fix.sh

      - name: Run checks
        run: bash ckad/31/assets/check.sh

      - name: Tear down
        if: always()
        run: bash ckad/31/assets/cleanup.sh
