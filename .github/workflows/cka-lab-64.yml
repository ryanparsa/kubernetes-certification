name: CKA Lab 64

on:
  push:
    paths: ['cka/64/**', '.github/workflows/cka-lab-64.yml']
  pull_request:
    paths: ['cka/64/**', '.github/workflows/cka-lab-64.yml']

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Create cluster
        uses: helm/kind-action@v1
        with:
          config: cka/64/assets/kind-config.yaml
          cluster_name: cka-lab-64

      - name: Install Calico
        run: |
          kubectl apply -f https://raw.githubusercontent.com/projectcalico/calico/v3.25.0/manifests/calico.yaml
          kubectl wait --for=condition=Ready nodes --all --timeout=300s

      - name: Apply solution
        run: bash cka/64/assets/fix.sh

      - name: Run checks
        run: bash cka/64/assets/check.sh

      - name: Tear down
        if: always()
        run: bash cka/64/assets/cleanup.sh
