name: CKA Lab 35

on:
  push:
    paths: ['cka/35/**', '.github/workflows/cka-lab-35.yml']
  pull_request:
    paths: ['cka/35/**', '.github/workflows/cka-lab-35.yml']

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Create cluster
        uses: helm/kind-action@v1
        with:
          config: cka/35/assets/kind-config.yaml
          cluster_name: cka-lab-35

      - name: Apply solution
        run: bash cka/35/assets/fix.sh

      - name: Run checks
        run: bash cka/35/assets/check.sh

      - name: Tear down
        if: always()
        run: bash cka/35/assets/cleanup.sh
