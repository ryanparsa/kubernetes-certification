name: CKAD Lab 30

on:
  push:
    paths: ['ckad/30/**', '.github/workflows/ckad-lab-30.yml']
  pull_request:
    paths: ['ckad/30/**', '.github/workflows/ckad-lab-30.yml']

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Create cluster
        uses: helm/kind-action@v1
        with:
          config: ckad/30/assets/kind-config.yaml
          cluster_name: ckad-lab-30

      - name: Apply solution
        run: bash ckad/30/assets/fix.sh

      - name: Run checks
        run: bash ckad/30/assets/check.sh

      - name: Tear down
        if: always()
        run: bash ckad/30/assets/cleanup.sh
