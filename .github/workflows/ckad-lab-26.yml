name: CKAD Lab 26

on:
  push:
    paths: ['ckad/26/**', '.github/workflows/ckad-lab-26.yml']
  pull_request:
    paths: ['ckad/26/**', '.github/workflows/ckad-lab-26.yml']

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Create cluster
        uses: helm/kind-action@v1
        with:
          config: ckad/26/assets/kind-config.yaml
          cluster_name: ckad-lab-26

      - name: Apply solution
        run: bash ckad/26/assets/fix.sh

      - name: Run checks
        run: bash ckad/26/assets/check.sh

      - name: Tear down
        if: always()
        run: bash ckad/26/assets/cleanup.sh
