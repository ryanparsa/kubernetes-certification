name: CKAD Lab 23

on:
  push:
    paths: ['ckad/23/**', '.github/workflows/ckad-lab-23.yml']
  pull_request:
    paths: ['ckad/23/**', '.github/workflows/ckad-lab-23.yml']

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create cluster
        uses: helm/kind-action@v1
        with:
          config: ckad/23/assets/kind-config.yaml
          cluster_name: ckad-lab-23

      - name: Apply solution
        run: bash ckad/23/assets/fix.sh

      - name: Run checks
        run: bash ckad/23/assets/check.sh

      - name: Tear down
        if: always()
        run: bash ckad/23/assets/cleanup.sh
