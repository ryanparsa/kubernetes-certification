name: CKA Lab 37

on:
  push:
    paths: ['cka/37/**', '.github/workflows/cka-lab-37.yml']
  pull_request:
    paths: ['cka/37/**', '.github/workflows/cka-lab-37.yml']

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Create cluster
        uses: helm/kind-action@v1
        with:
          config: cka/37/assets/kind-config.yaml
          cluster_name: cka-lab-37

      - name: Apply solution
        run: bash cka/37/assets/fix.sh

      - name: Run checks
        run: bash cka/37/assets/check.sh

      - name: Tear down
        if: always()
        run: bash cka/37/assets/cleanup.sh
