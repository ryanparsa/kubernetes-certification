name: CKAD Lab 43

on:
  push:
    paths: ['ckad/43/**', '.github/workflows/ckad-lab-43.yml']
  pull_request:
    paths: ['ckad/43/**', '.github/workflows/ckad-lab-43.yml']

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Create cluster
        uses: helm/kind-action@v1
        with:
          config: ckad/43/assets/kind-config.yaml
          cluster_name: ckad-lab-43

      - name: Apply solution
        run: bash ckad/43/assets/fix.sh

      - name: Run checks
        run: bash ckad/43/assets/check.sh

      - name: Tear down
        if: always()
        run: bash ckad/43/assets/cleanup.sh
