# Question 39

> **Solve this question on:** `cka-lab-39`

Create a static pod named `static-web` on the node using the `nginx:1.19` image.

The pod should expose port `80`.

Place the static pod manifest file at `/etc/kubernetes/manifests/static-web.yaml`.

---

**Setup:** `bash assets/setup.sh` - **Cleanup:** `bash assets/cleanup.sh`
