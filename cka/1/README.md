# Question 1

> **Solve this question on:** the "cka-lab-1" kind cluster

You're asked to extract the following information out of kubeconfig file `cka/1/lab/kubeconfig`:

1. Write all kubeconfig context names into `cka/1/lab/contexts`, one per line
2. Write the name of the current context into `cka/1/lab/current-context`
3. Write the client-certificate of user `account-0027` base64-decoded into `cka/1/lab/cert`

---

**Setup:** `bash assets/setup.sh` - **Cleanup:** `bash assets/cleanup.sh`
