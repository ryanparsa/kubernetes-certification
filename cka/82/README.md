# Question 82

> **Solve this question on:** the `cka-lab` kind cluster

There are two *Pods* named `o3db-*` in *Namespace* `project-tiger`. The team asked you to scale these down to one replica to save resources.

> [i] The Pods are managed by a Helm release. Scale using `kubectl` rather than modifying Helm values directly.
