# Question 180

> **Solve this question on:** `ssh cka412`

You're asked to extract the following information out of the kubeconfig file `/opt/course/1/kubeconfig` on `cka412`:

1. Write all kubeconfig context names into `/opt/course/1/contexts`, one per line

1. Write the name of the current context into `/opt/course/1/current-context`

1. Write the client-key of user `account-4827` (base64-decoded) into `/opt/course/1/cert`
