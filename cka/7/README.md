# Question 7

> **Solve this question on:** the "cka-lab-7" kind cluster

The metrics-server has been installed in the cluster. Write two bash scripts which use `kubectl`:

1. Script `cka/7/lab/node.sh` should show resource usage of nodes
2. Script `cka/7/lab/pod.sh` should show resource usage of Pods and their containers

---

**Setup:** `bash assets/setup.sh` - **Cleanup:** `bash assets/cleanup.sh`
