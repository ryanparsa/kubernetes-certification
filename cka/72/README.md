# Question 72

> **Solve this question on:** the "cka-lab-72" kind cluster

You have access to multiple clusters from your main terminal through `kubectl` contexts. Write all those context names into `/opt/course/72/contexts`.

Next write a command to display the current context into `/opt/course/72/context_default_kubectl`. The command should use `kubectl`.

Finally write a second command doing the same thing into `/opt/course/72/context_default_sh`, but without the use of `kubectl`.

---

**Setup:** `bash assets/setup.sh` - **Cleanup:** `bash assets/cleanup.sh`
