# Question 93

> **Solve this question on:** `docker exec -it cka-lab-control-plane bash`

Perform the following tasks on cluster certificates:

1. Check how long the Kubernetes API server certificate is still valid. Write the expiration date in format `mm/dd/YYYY` into `/opt/course/14/expiration`

1. Write the `kubeadm` command that would renew the Kubernetes API server certificate into `/opt/course/14/kubeadm-renew-certs.txt`
