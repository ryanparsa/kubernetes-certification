apiVersion: v1
kind: Service
metadata:
  name: service-am-i-ready
  namespace: default
  labels:
    id: cross-server-ready
spec:
  selector:
    id: cross-server-ready
  ports:
  - port: 80
    targetPort: 80
