# Question 106

> **Solve this question on:** `kubectl config use-context k8s-c1-H`

The metrics-server has been installed in the cluster. Your job is to find the *Pod* and *Node* using the most resources.

Write the name of the *Pod* using the most CPU into `/opt/course/7/pod.txt`.

Write the name of the *Node* using the most memory into `/opt/course/7/node.txt`.
