# Question 136

> **Solve this question on:** `kubectl config use-context k8s-c1-H`

The metrics-server has been installed in the cluster. Your colleague would like to know the following commands:

1. Show *Nodes'* resource usage
2. Show *Pods* and their containers' resource usage

Please write the commands into `/opt/course/7/node.sh` and `/opt/course/7/pod.sh`.
