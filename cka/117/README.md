# Question 117

> **Solve this question on:** `kubectl config use-context k8s-c1-H`

There seems to be an issue with the `kubelet` not running on `cluster1-worker1`. Fix it and confirm the `kubelet` is running. The node should be available in `Ready` state afterwards. You should be able to schedule a *Pod* on `cluster1-worker1` afterwards.

Write the reason of the issue into `/opt/course/18/reason.txt`.
