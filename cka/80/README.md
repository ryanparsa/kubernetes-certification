# Question 80

> **Solve this question on:** the `cka-lab` kind cluster

You're asked to extract the following information from the cluster and write it into files under `/opt/course/1/`:

1. Write all pod names running in all namespaces, one per line, into `/opt/course/1/pods.txt`

1. Write all running container image names from all pods in all namespaces, one per line (deduplicated), into `/opt/course/1/containers.txt`

1. Write the name of the current context into `/opt/course/1/context`

1. Write the client certificate of user `accounts-432` (decode it and write only the certificate string) into `/opt/course/1/cert`
