# Task 22 — Cluster Architecture: CSR Approval

**Context:** Cluster `cka-task-22` (`export KUBECONFIG=$PWD/kubeconfig`)

A developer named `john` has submitted a Certificate Signing Request (CSR) to gain
access to the cluster.

## Objective

1. Identify the CSR named **`john-developer`**.
2. **Approve** the CSR so that the certificate is issued.

## Verify

```
./test.sh
```
