# Question 149

> **Solve this question on:** `kubectl config use-context k8s-c2-AC`

Your coworker node `cluster2-worker1` is running an older Kubernetes version and is not even part of the cluster. Add the node to the cluster using `kubeadm` and TLS bootstrapping.
