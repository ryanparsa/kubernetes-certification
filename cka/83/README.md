# Question 83

> **Solve this question on:** the `cka-lab` kind cluster

Check all available *Pods* in *Namespace* `project-c13` and find the names of those that would probably be terminated first if the *Nodes* run out of resources (cpu or memory).

Write the names of those *Pods* into `/opt/course/4/pods.txt` (one per line).

> [i] Consider QoS classes to determine which Pods are terminated first under resource pressure.
