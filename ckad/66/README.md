# Question 66

> **Solve this question on:** `ckad-lab-66`

A deployment called `nginx-deployment` exists in namespace `set-env-namespace`. It has an environment variable `TIER=web` set on its containers.

Update the deployment so that the environment variable `TIER` is changed to `app`.

---

**Setup:** `bash assets/setup.sh` - **Cleanup:** `bash assets/cleanup.sh`
