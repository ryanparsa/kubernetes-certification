# Question 137

> **Solve this question on:** `ckad-lab-18`

There seems to be an issue in *Namespace* `mars`. There is a *Deployment* named `mars-1274`. Check if there is a problem and fix it.

You can test the fix by running `curl` on the *Service* `mars-svc` from inside the cluster.

Check if there is a `CrashLoopBackOff` situation and fix the underlying cause. Write a short description of the problem to `/opt/course/18/reason.txt`.
