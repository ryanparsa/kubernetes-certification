# Question 57

> **Solve this question on:** `ckad-lab-57`

Create a namespace called `pod-namespace`.

Create a pod called `pod-1` using the `nginx` image. The container inside the pod must be named `container-1`.

---

**Setup:** `bash assets/setup.sh` - **Cleanup:** `bash assets/cleanup.sh`
