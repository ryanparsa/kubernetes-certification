# Question 63

> **Solve this question on:** `ckad-lab-63`

Create a namespace called `log-namespace`.

Create a pod called `log-pod` using the `nginx` image in namespace `log-namespace`.

Retrieve the logs for `log-pod` from the **last hour** only.

---

**Setup:** `bash assets/setup.sh` - **Cleanup:** `bash assets/cleanup.sh`
